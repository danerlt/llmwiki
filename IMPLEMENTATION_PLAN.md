# Lumen 企业级知识平台 — 实施计划

> 目标：把 LLM-Wiki MVP 演进为真实可售的企业级知识平台（对标 Confluence/Notion/Guru + AI 检索增强）。
> 流程：每阶段 3–5 步，TDD（红→绿→重构），逐步提交。完成的阶段在此勾掉，全部完成后删除本文件。

---

## 阶段 1：安全与正确性加固（地基）— ✅ 已完成

**目标**：修复多智能体对抗式审计发现的 13 条确认问题（多租户越权/数据泄漏/一致性/鉴权），把地基夯实再加特性。
**成功标准**：每条发现有对应回归测试；全量后端 101 + 前端 5 测试绿。

| # | 严重度 | 问题 | 修复 | 状态 |
|---|---|---|---|---|
| 1 | critical | 晋升 request/approve 不校验作用域→跨作用域泄漏/投毒 | 申请时要求 to_kb 在申请人可见域 + 审批时按申请人可见域纵深复核 | ✅ |
| 2 | critical | JWT 弱默认密钥仅在 `app_env=='prod'` 精确匹配时拒绝 | 非 dev/test/local 的任何环境一律拒绝弱凭据 + dev 告警 | ✅ |
| 3 | medium | `get_page` 来源文件名未按可见域过滤→跨 KB 泄漏 | 与 backlinks/outlinks 一致按 accessible 过滤 sources | ✅ |
| 7 | medium | 摄入失败 `str(exc)`(含 SQL/路径/密钥)透传给读者 | 只存脱敏错误码，完整异常仅进服务端日志 | ✅ |
| 9 | medium | 先提交 pending 再入队，入队失败源永久卡死 | `_enqueue_or_fail`：入队失败置 failed + 503 可重试 | ✅ |
| 12 | low | org 写接口缺 max_length→超长输入 500 | Field(max_length) 在边界以 422 拒绝 | ✅ |
| 10 | medium | 上传先全量 read 再判大小→DoS | 1MB 分块读取、随读随校验、超限即 413 | ✅ |
| 13 | low | /query、/search 入参无长度上界→LLM 成本放大 | Field(max_length=2000)/Query(max_length=500) | ✅ |
| 6 | medium | 同 source 并发处理→唯一约束冲突假失败 | `source_repo.claim` 原子认领（行级锁去重） | ✅ |
| 4 | medium | 摄入异常被吞→arq 重试形同死代码 | 瞬时故障(LLM/存储)写 failed 后重抛→arq 重试 | ✅ |
| — | (存疑) | processing 仅内存未提交→外部读到 pending | claim 后立即提交 processing，可观测 + 互斥 | ✅ |
| 5 | medium | reingest 非幂等→孤儿页残留 | 写完新页清理本 source 上轮产出、本次未生成的页 | ✅ |
| 8 | low-med | JWT 无吊销 + 12h 有效期 | `token_version` 会话吊销 + `/auth/logout` + 有效期 480min | ✅ |

**审计方法**：6 维度并行发现 → 每条双视角对抗核验（可触发性 + 是否已被覆盖），13/15 确认。

---

## 阶段 2+：企业能力路线图

> 来源：`enterprise-roadmap-planning` 工作流的【现状盘点 + 6 视角】产出（综合 agent 因输出体积超限卡死，
> 综合由主导 Claude 基于已恢复的盘点+5 视角+领域知识完成）。盘点确认现有 9 大能力域，并列出 16 项企业级缺口。
> 排序原则：依赖关系 → 企业价值 → 落地成本。✅完成 / 🚧进行中 / ⬜未开始。

### 阶段 2：平台基线与可运维性 🚧
**目标**：补齐企业采购“必查项”的 Web/运维基线，作为后续一切特性的地基。
- 🚧 安全/运维中间件：✅安全响应头 ✅CORS ✅request_id 贯穿 / ⬜限流(rate limit) ⬜TrustedHost
- ✅ 全局异常处理（统一错误体，不泄漏堆栈/SQL，带 request_id）✅ 结构化日志 + 请求访问日志（method/path/status/dur + request_id）
- ✅ 深度健康检查：`/health`(存活) + `/readyz`(探 DB/Redis/MinIO 就绪，任一不通 503)
- ✅ Prometheus `/metrics`（请求量 + 延迟 + LLM 调用数/token 计量）/ ⬜ 队列积压
- 🚧 列表接口统一分页：✅审计(envelope+总数+动作过滤+加载更多) / ⬜ pages/sources（受 KB 体量约束，暂留）
- ✅ LLM 网关健壮化：可配超时 + 429/5xx/超时指数退避重试 + 4xx 不重试 + token 计量 / ⬜ 熔断
- ⬜ API 限流（rate limit）

### 阶段 3：内容创作与版本（Confluence/Notion 对标，最大产品缺口）🚧
**目标**：让人能直接创建/编辑/删除页与查看历史——当前页面只能由 LLM 摄入生成，这是对标产品的头号缺口。
- ✅ 人工页 CRUD：POST/PUT/DELETE，受 can_write 约束；slug 唯一(409)、系统页类型拒绝、wikilink 重建、审计
- ✅ 前端 Markdown 编辑器（创建/编辑/删除 + 实时预览，复用渲染）
- ✅ 版本历史 + 回滚（page_versions 表，每次保存留版本；前端历史页查看+回滚）/ ⬜ 行级 diff 高亮
- ⬜ 草稿 → 评审 → 发布 状态机（避免半成品污染检索/RAG）
- ⬜ 软删除 / 回收站 / 恢复（当前为硬删除）

### 阶段 4：协作 ⬜
**目标**：把散落 IM 的讨论沉淀回知识，构建留存命脉。
- ⬜ 领域事件总线 + 通知中枢；站内通知收件箱
- ⬜ 页面评论/讨论线程；@提及（人员/页面补全）
- ⬜ 页面/KB 订阅(watch) + 变更通知（站内，邮件渠道可选）
- ⬜ 标签/收藏/活动流；内容认证与过期复审提醒

### 阶段 5：AI 与检索升级（让“检索增强”名副其实）⬜
**目标**：从关键词召回升级为语义/混合检索，问答体验对标商业产品。
- ⬜ 向量嵌入基础设施（embedding 服务 + pgvector 向量存储 + 增量更新）
- ⬜ 混合检索（trgm/BM25 + 向量 → rerank）
- ⬜ SSE 流式问答；多轮对话（会话上下文 + 查询改写）
- ⬜ 答案反馈闭环（赞/踩）+ 片段级溯源 + 置信度 + 防幻觉 grounding 校验
- ⬜ 摄入期自动富集：摘要/标签/实体持久化；相关页语义推荐

### 阶段 6：企业身份与数据治理 ⬜
**目标**：跨过 B 端采购的身份/合规硬门槛。
- ⬜ SSO（OIDC + SAML 2.0）；SCIM 2.0 用户/组 provisioning
- ⬜ 细粒度自定义 RBAC + 资源级 ACL（KB/页级共享、只读成员）
- ⬜ 会话加固续作：refresh token、密码重置/修改、登录失败锁定、（可选 MFA）
- ⬜ 数据治理：保留策略、GDPR DSAR（导出/被遗忘权）、备份恢复、审计防篡改+过滤导出

### 阶段 7：发现、分析与体验 ⬜
**目标**：让知识被用起来，并向采购方证明 ROI。
- ⬜ 高级搜索：分面/过滤(作用域/类型/时间/库)、Cmd-K 命令面板、中文纠错与即时建议
- ⬜ 行为埋点底座 → 使用分析仪表盘（活跃/热门/趋势）、搜索无果词/知识空缺分析、内容健康度
- ⬜ 个性化首页/推荐；i18n 多语言；无障碍(a11y) 与移动端响应式

### 阶段 8：外部集成与多租户 ⬜
**目标**：从孤岛变成企业事件枢纽，并支撑多客户 SaaS。
- ⬜ 对外 REST API + API Key/服务账号；Webhook 事件订阅
- ⬜ 多租户隔离层（organization/tenant）
- ⬜ 更多数据源：docx/pptx/xlsx/html/网页抓取/Confluence 导入；扫描件 OCR
- ⬜ CI/CD 流水线与质量门禁（消除 SQLite↔PG 行为漂移，真实 PG 集成测试）
